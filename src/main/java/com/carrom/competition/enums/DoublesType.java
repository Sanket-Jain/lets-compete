package com.carrom.competition.enums;

public enum DoublesType {
    /** Both players same gender (Carrom, Badminton same-sex doubles) */
    SAME_GENDER,
    /** One male + one female (Badminton mixed doubles) */
    MIXED
}
